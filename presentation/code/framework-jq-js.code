let names = ['Tom', 'Dick', 'Harry'];

$('#search').onChange(search => {
  let filteredNames = names.filter(n =>
      n.indexOf(search) > -1);

  $('#names').html(
    filteredNames
         .map(name =>
             '<li>' + name + '</li>')
         .join()
  );
});
