// lib/math.js
export function sum(x, y) { // visible from outside
  return x + y;
}
export var pi = 3.141593; // visible from outside
var secret = 'Secret'; // not visible from outside

// app.js
import * as math from "lib/math";
alert("2π = " + math.sum(math.pi, math.pi));

// otherApp.js
import {sum, pi} from "lib/math";
alert("2π = " + sum(pi, pi));
